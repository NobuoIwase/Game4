# ルミナ・サバイバーズ — 引き継ぎ資料

**このファイルは、この作品に初めて触れるAI(および人)のための入口です。**
作業を始める前に、まずここを通してから、下の「最初に読むもの」へ進んでください。
新しい版を出すたびに、このファイルの末尾の履歴と「既知の課題」を更新します。

- 公開先: https://nobuoiwase.github.io/Game4/
- 作業ブランチ: `claude/ai-girl-deck-battle-game-1k94l5`(push すると GitHub Pages が更新される)
- 最終更新: v6.4b

---

## 0. 最初に読むもの(順番どおりに)

| 順 | ファイル | なぜ |
| --- | --- | --- |
| 1 | **`統合嗜好プロファイル (2).md`** | **最重要。** 作者の嗜好と執筆仕様の聖書。50KB。エロ本文・キャラ設計・システム設計の全ての判断基準がここにある。「何が好きか」のリストではなく、**なぜそれが刺さるのか**の推論と、**ではどう書くのか**の書式仕様まで含む |
| 2 | `汎用カタログ_状態異常・性癖・ステータス.md` | 状態異常・性癖・記録項目の網羅カタログ。実装との突き合わせに使う |
| 3 | この `docs/BRIEFING.md` | 全体像・コードの地図・作法 |
| 3.5 | **`docs/MAP.md`** | **逆引き**: 「〜したい」→ 触る場所 / データの形 / 検証の雛形。実装を始める前にここで当たりを付ける(参照は `check_map.py` で検証されている) |
| 4 | `README.md` | 版ごとの実装内容(141KB)。「その機能がなぜそうなっているか」はここ |
| 5 | `docs/DESIGN.md` | 設計仕様(184KB)。関数名・定数名つきの詳細。**実装を触る前に該当節を引くこと** |
| 6 | `docs/STORY.md` / `docs/TEXT_SPEC_v30.md` | 物語の下敷き / 執筆仕様(声の文法・キー一覧・NG) |

> **プロファイルの最重要の一行**
> 「この作者のエロは、行為ではなく **『まだ抗っている心が、快感に軋んでいく過程の分解能』** である。
> 絶頂は句読点であり、本文は『耐えている間』。」
>
> 迷ったら第三部-B の実例に戻ること。**堕ち切った常態の描写は薄くてよい。**

---

## 1. これは何か

**R-18のブラウザゲーム。** HTML+素のJavaScript(フレームワーク無し)。個人開発・非商用。

ヴァンパイアサバイバーズ風の見下ろし戦闘だが、**操作するのは魔物(夜)の側**。
ヒロインたちは完全にAIで動き、プレイヤーは彼女たちを**倒す側**として、
魔物カードを場に出し、罠を置き、陣形を選ぶ。

- **ヒロインは操作できない。** プレイヤーの入力は一切ヒロインに届かない
- 勝利条件(夜側) = ヒロイン全員を捕獲する
- 敗北条件(夜側) = ヒロインが深淵の底の「魔核」を討つ

### 遊びの骨格

```
ホーム → 編成 → 出撃 → [1日ぶんの戦闘] → 結果 → 研究所/祭壇で強化 → 翌日 …
```

- **1日 = 1階層**。ヒロインが降り口に着けば次の階へ、捕まれば夜側の勝ち
- **階層は15まで**。開放数 `openFloors() = min(15, 2 + 世代)`。
  13〜15 は「特化階層」——一階につき一つの問いだけに賭ける
  (13 忘却 = 記憶は消えて身体は残る / 14 待ち = 追ってくるものが一体も居ない /
  15 抵抗 = 抗いが全部そのまま責めになる)
- 最終階層に **魔核(心臓)** が居る。討たれると世代が1つ進み、階層が1つ開く

---

## 2. ★物語の芯 — お互い忘れる巻き戻し合戦

**ここを外すと、この作品ではなくなります。**

巻き戻しは二種類あり、どちらも **★忘れるのは「倒した側」です**。
負けた側が時を巻き戻し、その代金として**勝った側の記憶**が消えます。

| | ルミナが勝つ(魔核を討つ) | 心臓が勝つ(二晩つづけて捕まる) |
| --- | --- | --- |
| 引き金 | 魔核を討たれる | 二晩つづけて捕まる |
| 世界 | 一日目の朝へ戻る | 一日目の朝へ戻る |
| 時を巻き戻すのは | 負けた**心臓** | 負けた**彼女たち** |
| **忘れるのは(=倒した側)** | 勝った**彼女たち**。何も知らない朝に立つ | 勝った**心臓**。二晩ぶんを知らない |
| 覚えているのは | 心臓 | 彼女たち |
| 外に残るもの | **心臓の身が一段厚くなっている** | **仲間が一人増えている** |
| その痕跡 | 自分の身の厚み(心臓は自分では読めない) | 石段に刻んだ線(彼女たちは自分で数えられる) |

**芯**: 倒した側は代償として記憶を失い、その代わり「外に残る一つ」だけが世界に刻まれる。
そして **その痕跡は、忘れた本人には読めない**——自分が勝った証だと分からない。

★実装での対応(`runReset(wipeKnow)`):
`outcome==='clear'`(ルミナ勝利)は `runReset(true)` で**彼女たちの**覚えたことが消える。
二連敗は `runReset(false)` で彼女たちの知識は残る——忘れているのは心臓の側。

- 彼女たちが見るもの: 話に聞いたより厚い心臓。「もっと弱いはずなのに」。
  自分たちが討った回数ぶん厚いのだと**知らない**
- 心臓が感じるもの: あと一晩で落とせたはずの相手が、数を増やして戻ってくる。
  自分が勝った回数ぶん増えたのだと**知らない**
- **不変量: 「増えた/大きい」と口に出すのは、いつも忘れた側。** 覚えている側は説明がつくので言わない
- **数えられるのは地の文(`n:`)だけ。** 語り手だけが両方の帳簿を見ている

### 書く時の禁則

1. **どちらの側も「ループ」「巻き戻し」という概念を理解していない。**
   彼女たちが「戻された」と分かるのは二連敗の朝だけ(記憶が残るので)。
   魔核を討った翌朝は**本当に何も知らない一日目の朝**として書く
2. **説明台詞にしない。** 「私たち、ループしてるんだ!」は絶対に書かない
3. **反復は最大の敵**(プロファイル推論4)。同じ言い回しを二度使わない
4. 心臓の声は `voice` 話者。短く、途切れがちで、**主語が無い**

---

## 3. 登場人物

| | 声の核 | **他の三人との間柄** | 崩れ方 | 禁止語 |
| --- | --- | --- | --- | --- |
| **ルミナ** 光の見習い天使 | 元気っ子。「〜だよ/だし/もん/じゃん」。ひらがな多め | 主人公。誰にでも先に踏み込む | 口語の軽さが壊れて**幼い素**が出る。「〜だし」が消えて素の悲鳴に | 武家語・古語・祈り言葉 |
| **フレイラ** 火の近接天使 | 短く言い切る。命令形。矜持が高い | ルミナの相棒。前に立ちたがる | 言い切ろうとして途中で切れる | **「だわ」「〜よ」「かしら」「わね」** |
| **クウ** 氷の見習い天使 | 無口・クール・少しませてる。一文が短い | **ルミナとフレイラの友達。二人から妹あつかいされる**。フレイラの隣だけは苦手(近いと溶ける) | ふふん、が言えなくなる | 「だわ」「のよ」「かしら」「わね」/「だよ」「だし」「もん」 |
| **ヤミコ** 闇 | カッコイイ系。お姉さんを振る舞う。夜側の知識を持つ | **唯一のよそ者**。渦の中心で生まれ、助けられて付いてきた。三人とは後から知り合う | **敬語が出る**(「見ないで、ください」) | 同上の女性語尾 |

★**「よそから来た、名前も知らない者」の枠はヤミコのものであって、クウのものではない。**
v6.2 まで、クウの合流の場面が初対面として書かれていた(「え、だれ?」「……名前は」「よろしくね、クウ」)。
間柄は `js/data.js` の素性コメントに「妹あつかい」と一行あるだけで**この表に無く**、
物語を書く時に参照されずに落ちた。だからこの欄を作った。
なお**距離を取る振る舞いは正しい**——`heatShy:true` でフレイラのそばでは氷の半径が縮む。
直すのは間柄であって、距離ではない。

**全員共通**: 典型的女性語尾(〜だわ、〜のよ)は禁止。**ヒロインは絶対に「催眠」と口にしない**。
♡は最も深い快楽の瞬間だけ(語尾連発NG・地の文には書かない)。三点リーダは「……」。

### 合流(v5.5 以降)

仲間は**深さではなく巻き戻りで増える**。
二連敗で時が戻された朝に、前の周には居なかった子が居る。

- 1回目の巻き戻り → **フレイラ**
- 2回目の巻き戻り → **クウ**
- **ヤミコだけ別ルート**: クウ参戦後、前回の最下層に**眠り手**として現れる → 次の周でその階で
  淫魔たちに囲まれている所を助けられる → その次の周の最初から参戦

---

## 4. システムの地図

| 系統 | 中身 |
| --- | --- |
| **状態異常** | 敏感化 / 発情Ⅰ〜Ⅲ / 魅了Ⅰ〜Ⅲ(種族別) / 催眠Ⅰ〜Ⅲ / 淫紋Ⅰ〜Ⅲ / 寸止め / 疼き / 痺れ / 憑依 / 視姦 / 時間停止 / 触手服 / おねだり / 淫糸 / 自慰 / 強制絶頂 |
| **拘束** | 四肢(左右の腕・脚)＋吸い付き(胸左右・秘部)。もがいて振りほどく。スタミナが薄いと**押し倒し**へ |
| **絶頂** | 快感100で絶頂。v5.7 から**連続絶頂**(絶頂中も100に届けば重なり、重なるほどスタミナを持っていく) |
| **学習** | 種族ごとに met/cap を数え、`knowLv` が上がると回避・狙い・地形の扱いが変わる。**世代が巻き戻ると消える** |
| **暗闇** | 基本は暗い。本人が淡く光る。光・炎の通った所がしばらく視界に残る |
| **地形** | ゾーン(苔/湿地/水/花/肉/巣/遺跡/温泉/媚薬の澱み)＋設計地形(柱の間・崖の段・闘技場・迷路・喉道・巣窟) |
| **巣窟(えちえちエリア)** | 壁際の広い窪地。入口ひとつ。奥に王の宝箱。魔法陣・ガスの花・壁の光線・固有の番人 |
| **パーティAI** | 行き先の相談 / 追従・隊形 / カバー / 救出 / 情報共有 / 巣窟で待つか入るか |
| **経済** | エッセンス(魔物の解放・強化)/ オーブ(夜側の永続強化・ヒロインの弱体化)/ ルミナのコイン(自己強化) |
| **淫魔の指揮(v6.6)** | 淫魔は一個下の階級を速さと手数で強化する(`DEMON_CMD`/`demonCmdAt`)。責めは持たない |
| **絶頂禁止/おあずけ(v6.6)** | 絶頂禁止=栓をして溜めさせる(`h.denyOver`)。おあずけ=寸前で引き戻す**状態**(`h.omazukeHold`)。三度で許しが出る |
| **眼の条(v6.6)** | 壁に当たるまで流れ続ける光(`rayStep`)。浅い階は99止まり、`RAY_DEEP` 層から連続絶頂へ |
| **ハイ/中毒(v6.6)** | 粉を吸う→速くなる→抜けて疲れる→中毒→自分からきのこへ(`highTick`/`addictSeek`) |
| **ボスの段(v6.6)** | `bossRank()` が会った深さで段を返し、能力そのものが増える(★`u.rank` とは別の `u.brank`) |
| **系統(v6.5)** | 魔物を六系統(ヌメリ/触手/淫魔/眼/胞子/霊)に括る。同じ系統でデッキを固めるほど、その系統だけカードCDが縮む(`FAMS`/`famOf`/`deckFam`)。届く世代は系統ごとに違う(世代0はヌメリ・触手だけ、世代4で六系統) |

---

## 5. コードの地図

読み込み順は `index.html` に書いてある。**すべて素の `<script>` で、グローバル変数を共有する**。

| ファイル | 役割 |
| --- | --- |
| `js/core.js` | 基盤。`G`(実行時状態)・`META`(セーブ)・音・ユーティリティ |
| `js/data.js` | **全定義データと `BAL`(バランス定数)**。魔物・武器・階層・ヒロイン・強化表 |
| `js/game.js` | **戦闘の本体(約7,500行)**。ヒロインAI・魔物の挙動・状態異常・パーティ・魔核 |
| `js/map.js` | 地形生成・ゾーン・POI・ミニマップ |
| `js/render.js` | 描画 |
| `js/ui.js` | DOM画面(ホーム/編成/研究所/祭壇/図鑑/観測記録/物語) |
| `js/main.js` | 起動とメインループ |
| `js/scenes*.js` | **押し倒し・魅了拘束・敗北の本文**(ヒロイン別) |
| `js/lines*.js` | マップ上の台詞(ヒロイン別＋パーティ) |
| `js/story*.js` | 物語(ADV形式 `{s,t}`) |
| `js/codex*.js` | 図鑑・**各人の手記**・称号。`codex.js`=ルミナ / `codex_freila|kuu|yamiko.js`=各人の帳面 / `codex_status.js`=ルミナ以外の総評・自己評価 |

### よく触る関数

- `aiDecide(foc,dt)` … ヒロインの行き先と向きを決める。**思考の拍(0.1〜0.3秒)ごと**
- `aiUpdate(dt)` … 毎フレーム。表示ラベル・グルグル検知
- `updateGoal(p)` … 目当ての採点と相談
- `attachMonster(mon,kind,opt)` … **全ての掴みの入口**。ここで注意力による回避判定
- `damageEnemy(e,dmg)` … 全ての敵ダメージの入口。剥がし・素性倍率
- `applyPleasure(amount)` … 快感の入口。連続絶頂の判定
- `heroLife(id)` / `heroRot(id)` … **その子の帳簿**(通算 / 世代内)。称号・堕ち二軸・意志・性癖はここから
- `codexMark(id,key,n,hero)` … 図鑑の記録。総体(`META.codex`)と**その子の頁**(`META.codexH[hero]`)の両方に積む
- `altarLvH(id,hero)` … ヒロインへの弱体化の段。夜側の軍備は `altarLv` のまま共通
- `heldTitles(hero)` … 称号。判定はその子の帳簿、名前と本文は `TITLE_BY[hero]` でその子の像に差し替える
- `battleTick(dt)` … 1フレーム

### 落とし穴(実際に踏んだもの)

- **`//` の行コメントを行の途中に入れると、その後ろが全部消える。** 必ず `/* … */` を使う
- **`BAL` のキー重複は黙って上書きされる。** `check_bal_dup.py` で毎回確認する
- **★通しの計測は分散が大きい。5本では詰められない。**
  v6.0 で実際に踏んだ: 同じコード・同じ設定で5本ずつ二度測ったら、
  削れた割合の中央値が10〜20点、討伐数が2つぶん動いた。
  効果より分散が大きいので、その差を根拠に定数を動かすと、ただの雑音を追うことになる。
  **15本以上取るか、討伐の有無ではなく「毎秒の削り」「生存秒」で測ること。**
- **★`wipeMeta()` で測ると、ありえないほど弱いパーティを測ることになる。**
  石段の線(`META.gen.marks`)は巻き戻しでも消えない痕で、最大で HP+48%・与ダメ+36%。
  ここを 0 のまま測って「世代4以降は魔核が討てない=第7〜15層が封鎖」と結論しかけた。
  ★ただし `marks ≒ 世代×0.8` と置くのも誤り——**線は「夜側が二連勝した回数」、
    世代は「彼女たちが心臓を討った回数」で、互いに逆を向く。**
  深い世代ほど線が多い、と置くと深層だけ不当に強くなる。
  計測では線を 0 と 6 の二点で挟んで、その帯で読むこと
- **敏感化には自然減衰(`SENSIT_DECAY` 1.1/秒)がある。**
  ここを下回る値を新しい責めに書くと、効きが丸ごと打ち消されて「死んだ定数」になる。
  v6.0 で実際に踏んだ: 設計どおり `MIR_SENS:0.9` と書いた鏡の床は、
  12秒立ち続けても敏感化の増分が **0.0** だった。減衰を差し引いた後の値を書くこと。
  ★v6.0f で**三度目**を踏んだ(`BREATH_SENS:2.2`。山の平均で 1.1/秒＝ちょうど打ち消し。
  336フレーム壁に擦れさせて 32→**30**、下がっていた)。9.0 に上げて解決。
  新しい責めの定数を書いたら、まず「一定時間立たせて増分を測る」を必ず一回やること。
  ★ただし**「増分を測る」だけでは足りない**。同じ壁ぎわの計測を4回やって
  +11 / +9 / +19 / **−16** と割れた——他の入り(その階の床・拘束・発情)が混ざるので、
  増減だけ見ても呼吸の効きは読めない。**同じ盤・同じ立ち位置で「その仕掛けを切った版」と
  引き算する**こと。切って測ったら 50→25.4(自然減衰だけ)、点けて測ったら 50→57.8。
  差 **+32.4 / 4周期(22.4秒)** がこの仕掛けの正味の効きで、そこまでやって初めて数字になる
  (`run_breath.js`)
- **★「定義がある」と「実際に刻まれる」は別。**
  性癖を9→25件に増やした最初の版は、走らせたら **25件中11件**しか刻まれなかった。
  閾値(何秒でひと刻み)を測らずに書いたから。**新しい積み上げを足したら、
  必ず「条件が1戦あたり何秒成立するか」を測ってから閾値を決める**(`run_traitcond61.js`)。
  実測: 待ち堕ち 24.0秒/戦・尖りの熱 23.5・見られ熱 9.4・溶ける 9.0・拘束癖 6.7・囃され 2.0、
  そして マゾの芯/律動従属/壺癖 は **一秒も成立しなかった**(条件そのものが厳しすぎた)
- **★溜まりを「離れたら0に戻す」と、歩く相手には一生届かない。**
  床の性癖は `else h.wombT=0;` のように、その床を離れた瞬間に溜まりを捨てていた。
  彼女は歩くので、同じ床に8秒続けて立つことが実プレイでほぼ無い——
  110戦で `mirrorT` は最大 **0.4**(閾値は7)。床の上に固定して測れば5回刻まれるので、
  仕組みは動いていた。**0 に戻すのではなく、`TRAIT_FADE` で少しずつ抜くこと**。
  ★「その床に立たせて測る」と「通しで測る」は別の問いで、前者だけでは穴が見えない
- **★監査スクリプト自身の思い込みを疑う。**
  文章の穴を数える監査を書いたら 70件出たが、**41件は監査側の誤り**だった——
  `UPG` は `desc` ではなく `d1`/`d2` を持つ。加えて `linesFor('lumina')` は
  **null を返す**(ルミナだけ共通の `LINES` を見る)ので、彼女だけ15階ぶん穴に見えていた。
  監査を信じて書き始めていたら、無い穴を埋めていた。
  **数える側も、実物を一度開いて確かめること**
- **★`META.run.floor` に深い階を入れても、`era` が低ければクランプされる。**
  `curFloorIdx()` は `openFloors()-1` で頭を抑える。`openFloors()` は `2+era` なので、
  **`era=2` では4階までしか開かない**。13〜15階を指定したつもりの計測が全部4階で走り、
  霜・胎・忘れ水・苔もどきの床に一度も立たないまま「その性癖は刻まれない」と読んでいた。
  ★深い階を測る時は `era` も一緒に上げること。この手のハーネス側の穴は、
  このセッションだけで**五度**踏んでいる(下の while、この era、監査の思い込み、
  「肢だけで数える」、そして「survived を回さない」)——
  **計測が期待と違ったら、まず計測側を疑う**
- **★ヒロインが四人いる盤で「肢」だけを鍵にして数えると、他人の腕と混ざる。**
  v6.2 の再拘束の猶予を検証したハーネスは、剥がした時刻を `armL` のような
  **肢の名前だけ**で覚えていた。ルミナの左腕を剥がした直後にフレイラの左腕が掴まれると、
  猶予破りとして数えてしまう。加えて `B.time` は戦闘ごとに 0 に戻るのに記録を持ち越していたので、
  20戦それぞれの頭でも誤検出していた。**447回中187回が猶予破り**という数字はこれで、
  鍵を「ヒロインid+肢」にし戦闘ごとに捨てたら **0回**。ゲーム側は最初から正しかった。
  **持ち主のいる状態を数える時は、鍵に持ち主を入れること**
- **★「絶対値の閾値」と「最大値に対する割合」を混ぜると、成立しない条件が黙って生まれる。**
  押し倒しは `PIN_STAMINA_TH:35`(**絶対値**)を下回ると起き、
  v6.2 の「えいっ」は `BURST_STAM:0.30`(**最大値の割合**＝30)以上を要求していた。
  両方を満たす窓が 30〜35 しかなく、押し倒し中はスタミナが減り続けるので、
  **12戦で押し倒しからの「えいっ」は一度も出なかった**。
  片方だけ読んでも気づけない。**単位の違う二つの門をくぐらせる時は、
  重なる帯が実際にどれだけあるかを書き出すこと**(押し倒し用に `BURST_STAM_PIN` を分けて解決)
- **★発動条件が保証する分を、素の値から引き忘れる。**
  「えいっ」の素の成功率 `BURST_BASE:0.74` は0本拘束のつもりで書いたが、
  `burstReady` が**3本以上でしか通さない**ので、`PER_LIMB×3 = -0.30` は常に払われる。
  実測は 3本 0.375 / 4本 0.294 で、狙いより遥かに低かった。
  ★この時、疑っていた「堕ち具合」と「余裕」は実測で 0.13 と 0.91、**ほぼ効いていなかった**。
  成分ごとに分けて測らなければ、当てずっぽうで別の定数を触っていた(`run_burst62.js`)。
  **門を通った後の分布を測って、そこを基準に素の値を置くこと**
- **★「定義がある」の次に確かめるのは「繋がっているか」。走らせなくても数えられる。**
  `hypnoObey` は刻む場所はあるのに**効きを読む場所が無かった**——
  取れるが何も起きない性癖。`sniffT`(嗅ぐ発作)は逆で、状態表示も反応も**4箇所**あるのに
  **代入する場所が一つも無く**、一生 0 のまま(専用の `MUSK_SNIFF` も置いてあった)。
  さらに `BAL` の **17件**が誰にも読まれていなかった
  (うち `IMP_TEASE_CAP` は `Math.min(2, …)` と literal に二重化していて、
  定数を動かしても効かない罠。`PICK_FAMILY_MAX` は「一階に1〜2株」と書いてあるのに常に1株)。
  どれも実測では出ない(出ても原因が分からない)。`python3 check_dead.py` を毎回。
  ★**この検査自身が四度間違えた**——(1)1行に複数ある鍵(`MIRE_R0:44, MIRE_R1:96,`)を
  先頭しか拾えず取りこぼし、(2)読む側は data.js の中にも居るのに data.js を丸ごと除外して
  `ERA_DEEP_FROM` などを誤検出し、(3)`BAL['CORE_SK_'+k]` の組み立て参照を見ておらず
  魔核の技を誤検出し、(4)`markTrait` を通さず `HL.traits.musk=lv` と直に書く道
  (`conditionMusk`)を見ておらず、雄臭を「刻む場所が無い」と誤って挙げた。
  ★この(4)は挙げた先が偶然当たっていた——雄臭は道こそあれ、その道が
  「熱を持ったまま雲の中で `muskCond` が25に達する」で、110戦で一度も通らなかった。
  **当たっていても、理由が違えば直し方も違う。**検査する側も検査すること
- **★状況証拠で直すな。条件を一つずつ数えろ。**(v6.2 で自分で踏んだ)
  14階で降りられない件で、「待ち手の道の座が降り口の際まで伸びているせいだ」と当たりをつけて
  `map.js` を触った。測り直したら 4/6 → 2/6 で、支持されなかった。
  そのあと**降下の門を一つずつ数えた**ら、落ちていたのは `exitGuarded()`(石の番兵)だった。
  ★さらに悪いことに、その前の計測で「封鎖=なし」と読んでいたのは
  `B.exitLocked`(封印石のパズル)という**別の旗**で、番兵の方は見ていなかった。
  **門が複数ある処理を疑う時は、門を全部並べて、どれが落ちているかを数えること。**
  一つだけ見て「通っている」と言わない
- **★閾値を「届く最大値と同じ」に置くと、刻まれるかどうかが運になる。**
  『絡まる安堵』は `silkT` の最大が **ちょうど 6.00**(閾値6)、
  『映る悦び』は `mirrorT` の最大が **ちょうど 7.00**(閾値7)で止まっていた。
  溜まるのは「ゆっくり歩いている間」「凪いだ鏡の上に立っている間」だけなので、
  越える手前で走り出す。刻まれるかどうかが毎回の運になり、
  150戦で 0〜1回という不安定さになる。**実測した最大値より下に置くこと**
  (`SILK_TRAIT_T:4.5` / `MIR_TRAIT_T:5.0`)
- **★刻まれた/刻まれなかった、を1回の計測で判断しない。**
  性癖のうち四つは 150戦で 0〜10回しか刻まれない稀な出来事で、
  **どれが落ちるかは回ごとに入れ替わる**(§7-4 の表)。
  一回の結果を見て定数を動かすと、直したつもりで別のものを落とす——実際にやった。
  三回まわして和を取ること
- **★写しを取ってから、輪の中でその写しの元を壊す。**
  `attachedSlots(h)` / `suckSlots(h)` が返すのは**その瞬間の写し**で、
  輪の中の `applyPleasure` が絶頂を起こすと、そこで別の肢や吸盤が外れる。
  次の回で `h.limbs[sl]` / `h.suckers[sl]` が null になっていて落ちる。
  肢では二度直してあったが、**吸盤側が残っていて v6.2 で落ちた**
  (`condTick`: Cannot read properties of null (reading 'mon'))。
  ★この例外は、通しテストが「レベルアップで抜ける」「survived を回さない」で
  途中までしか回っていなかったので、**それまで一度も表に出ていなかった**。
  計測の穴を塞ぐと、その先で本物の穴が待っている。
  **写しの要素を参照する時は、毎回引き直して null を確かめること**
- **★同期の `page.evaluate` の中では `ResizeObserver` が一度も走らない。**
  縦持ちのはみ出しを数えたら、AUTO・速度・撤退・アイテム・編成が
  **全機種で画面の下に 36〜47px 出ていて触れない**と出た。
  キャンバスの高さを戦闘バーに合わせ直すのは `ResizeObserver` の仕事で、
  それは**非同期**——同期の evaluate の中で組み立てて同じ evaluate で測ると、
  直る前の絵を測ることになる。間を空けて(`waitForTimeout`)から測り直したら
  三機種とも **0件**、手札も5枚とも画面内だった。
  ★DOM の寸法を測る時は、必ず一度ブラウザに時間を返すこと
- **★一日は `battle` だけでは終わらない。**
  降り口に立つと `startDescend()` が `G.mode='survived'` にするが、
  階を進める `endBattle('descend')` は**その後の `survivedTick` の中**にある。
  そこを回さずに `META.run.floor` を読んでいたので、260日走らせて
  「一度も降りていない(survive 260件)」に見えていた——**ゲーム側は毎日降りていた**。
  通しで回す時は `main.js` の場合分けと同じだけ回すこと
  (`battle` / `levelup` / `captured` / `survived`)。ハーネス側の穴はこれで**五度目**
- **★画面内に留める引き寄せ(`partyClamp`)が、壁を貫く唯一の経路だった。**
  「瞬間移動で分断される」を調べて奥義(光の跳躍・焔・影渡り)に `losClear` を足したのに、
  まだ壁越しが残った。x/y の書き込みに見張りを付けて呼び出し元を取ったら、
  **大きく跳ばしていたのは全部 `partyClamp`** ——二人を画面に収めるための引き寄せが、
  差分を一息に足して壁をすり抜け、向こう側で分断していた。
  奥義側は(この計測の間は)一度も撃っていない。
  **「何が起きたか」ではなく「誰が書いたか」を取れば、探し当てる必要がない**(`run_jump62.js`)。
  いまは `slideXY` で 6px ずつ刻み、壁に当たったら止める
- **★`while(G.mode==='battle')` は、レベルアップの瞬間に抜ける。**
  通しテストがこれで毎日レベルアップまでで打ち切られ、260日走らせて
  **一度も1階から出なかった**。ゲーム側は正常だった。
  日をまたぐ検証では `(G.mode==='battle'||G.mode==='levelup')` で回すこと
- **★動かない敵は、置いただけでは何もしない。**
  14階の法(追ってくるものを出さない)を「動く種を `spd=0` にする」で実装したら、
  実測で45秒・敵205体・彼女の発情 **0→0**。150秒走らせても、据わった相手には
  **123px までしか近づかなかった**——彼女のAIは敵を避けるので、
  間合いを持たない相手には一生触れない。
  据わらせるなら、**間合いを持つ種に置き換える**か、壁として手を伸ばさせること
- **★地形テンプレートは、広間(arena)を侵さないこと。**
  14階の待ち手の道は、座のまわり半径3〜6を埋める。座を降り口から7タイルの所に置いたので、
  **輪が魔核の間(半径10)へ食い込んで、party に遮蔽を与えていた**。
  同一セッションの baseline と比べて、世代12(＝最深が14階になる唯一の世代)だけ
  毎秒 280→412(線0)・301→456(線6)、討伐 2/8→6/8・3/8→7/8。壁越しに心臓を削れる盤だった。
  ★**構造で数えれば30秒で確定する**: 魔核の間(半径10タイル)の中の壁の枚数を数えると、
  道を引いた版は **47枚**、引かない版は **17枚**(隣の階は世代11で10枚・世代13で24枚)。
  難度曲線を25分回すより、**仕掛けが盤に何をしたかを直に数える**ほうが速くて強い。
  ★このバグは**測る向きで符号が変わる**ので厄介: 同じ壁が、
  「離れた所から心臓へ近づく」計測では毎秒を下げ(118→67)、
  「心臓の隣から始める」計測では毎秒を上げる。run_bal61 は隣から始めるので後者。
  **どちらの向きで測っているかを先に確かめること。**
  いまは最深階では道そのものを引かず、通常階でも広間の半径+余白ぶん離してから座を置く
- **★他所と比べる時は、同じセッションの baseline を取る。**
  「前の版では 270 だった」を別セッションの記録から引くと、機械の負荷も乱数も違う。
  `git worktree add <dir> <前の commit>` で並べて、同じセッションで両方走らせれば一度で決まる。
  実際これで「世代12の跳ねはブレではなく本物」と確定した(280→412)
- **到達性は「先に道を決めて、周りだけ埋める」。**
  壁で挟んでから `floodReach` で検査して駄目なら削る、という順は、
  検査に通るまで何回削るか読めない(＝生成が不安定になる)。
  14階の待ち手の道は、いまの盤で降り口までの経路を先に一本引き、
  その経路のタイルには絶対に触らずに周りだけ埋めている。構成として到達性が保たれる
- **`META.map` は一階ぶんしか持たない。** 階か世代が変われば `known`/`visited` は捨てられる。
  だから「一晩目の地図を15階で使う」ような**階を跨ぐ記憶は `META.gen` 側へ退避する**
  (v6.0f の `stashFirstNight`)。しかも退避は
  **`G.map` がまだ前の階のものである最後の瞬間**——`genMap` の冒頭——でしか取れない
- **生成の順番が、階の性格をそのまま決める。**
  v5.x では岩の形が先に空き地を埋めていたので、後から置く設計テンプレートが場所を取れず、
  「6本頼んだ狭窄が0本」のように階の顔だけが静かに消えていた。
  12階が実質3種類の形しかなかったのはこれが原因。**形は必ず数えて確かめる**
- **上位個体を作る時、`u.id` を変えてはいけない。**
  挙動の分岐(`e.id==='gtent'` など)が数十箇所あるので、id を変えると上位個体だけが
  既定の挙動に落ちる——しかも見た目では気づけない。
  v6.0 の熟れた個体は `u.id` に base を入れ、段は `u.rank` で持つ
- **代入の順番**。ファイル末尾で代入されるものを、前の行で参照すると `undefined` が焼き付く
  (v5.5 の合流バグがこれ)
- **`storyNorm` は「まだ仲間になっていない子の行」を落とす。** 物語は
  **ルミナと地の文だけで筋が通る**ように組み、他の子の行は足し算にする

---

## 6. 検証の作法

**この作品では「動かして数える」ことが強く求められます。** コードを読んだだけの推測は当てにならず、
実際に何度も、読みだけでは分からない穴(死んだ定数・到達不能な段階・順番の罠)が見つかっています。

### ヘッドレスで動かす

```bash
NODE_PATH=/home/user/Game4/node_modules node -e "..."   # Playwright
# executablePath:'/opt/pw-browsers/chromium-1194/chrome-linux/chrome'
# args:['--allow-file-access-from-files']
```

`page.evaluate` の中では `G` `META` `BAL` `UI` `MONSTERS` などを**裸の名前**で触れる(`window.X` ではない)。
`battleTick(1/60)` を同期で回せば、実時間より遥かに速く一日ぶんを回せる。

- `playsess.js` … 共通のプレイドライバ(git 管理外)
- `run_*.js` … 過去の検証ハーネス(git 管理外。`.git/info/exclude`)
- **ヘッドレスは速いフレームと遅いフレームが交互に来る。** 単発の計測は無意味。**必ず中央値で**

### コミット前に必ず

```bash
for f in js/*.js; do node --check "$f"; done     # 構文
python3 check_bal_dup.py                          # BAL のキー重複
python3 check_dead.py                             # 定義だけあって繋がっていないもの
python3 check_map.py                              # docs/MAP.md の参照が実在するか
# 文字集合スキャン(キリル文字などの混入。過去に複数回混入した)
```

### コミット規約

```
git -c user.name="Claude" -c user.email="noreply@anthropic.com" commit
```
本文の末尾に `Co-Authored-By:` と `Claude-Session:` を付ける。
**モデル名をコード・コミット・PRに書かないこと。**
push は `claude/ai-girl-deck-battle-game-1k94l5` へ。他のブランチへは押さない。

---

## 7. 版の履歴(要約)

詳細は `README.md` の各節に。

| 版 | 内容 |
| --- | --- |
| v0.3〜v1.5 | 拘束・スタミナ・押し倒し / 敏感化・発情・魅了 / 学習 / 描き込みグラフィック |
| v1.6〜v1.9 | 地形マップ / ボスと呪い / 抵抗の意志 / 目当て / 縦持ちレイアウト |
| v2.0〜v2.4 | **深淵(階層構造)と物語** / 設計された地形 / えちえちエリア / 奥義 / 視界の記憶 |
| v3.0〜v3.2 | **二人目フレイラとパーティAI** / 世代ループ / 洞窟の形 / 甘い褥の巣窟 |
| v4.0〜v4.1 | **暗闇** / 強化魔核 / フレイラの火属性と水弱点 / きのこと菌輪 |
| v5.0 | 階層9〜12 / **クウ(3人目)・ヤミコ(4人目)** / 媚薬沼 / 巻き戻りの向き |
| v5.1〜v5.2 | ヤミコの居場所を「前回の最下層」へ / 菌輪の名前衝突 / 魔核の絵の段階化 |
| v5.3 | 魔核から離れない / 研究所を横3マスの格子に |
| v6.6c | **咳き茸を床からも生やす**(菌輪の縁を優先。覚えて避けている輪の上に中毒の目当てが立つ)。★中毒の弧が通るまでに四つの穴を踏んだ: ①閾値1と値1.0が同時に成立しない(閾値は値より下に置く) ②`inSight` が厳しすぎた(中毒161秒のうち見えていたのは3.6秒) ③`add('addict')` は `updateGoalSolo` の中にあり、二人以上だと相談で毎回負けて消えていた——舞台試験が一人だったので通っていた ④踏む手前で刃が届いて壊してしまい一度も吸えない。0.0秒 → **45.4秒** |
| v6.6b | **淫魔系を現場指揮官に**(一個下の階級を速さと手数で強化。★e.spd はフレームごとに戻すこと)/ **寸止め→絶頂禁止に改名**し、溢れた快感が本当に溜まるように(コメントだけで実装が無かった)/ **女王のおあずけ**——★「出来事」では成立せず、見張る間ずっと堰き止める「状態」にして初めて回った / **眼系は壁まで流れる条へ**(浅い階は99で頭打ち、9層から連続絶頂。湧いた直後2.4秒は撃たない)+ 新雑魚 覗き子 / **胞子系のハイ→疲れ→中毒**+ 新雑魚 綿毛・咳き茸。★綿毛は本当に増えたが原因は分裂ではなく「オート指揮が安いカードを連打」——SPECIES_MAX で 65体→14体 / **ボスは会った深さで技が増える**(ボスゲイザーの目 3→6個)/ **ヴァンピロード削除** / ★計器の誤り: 「掴んだ回数」は指標として逆に動く(拘束時間で見る)・指揮官を真横に置くと体で塞ぐ |
| v6.6 | **催淫灯篭が一晩の発情の 1.2% しか作っていなかった**(そば 7.5秒/夜)。原因が三つ重なっていて、直す順に露出した——①3.4秒の打ち切りは滞在を切っていたのではなく**滞在そのものだった**(外したら 2.0秒に減った) ②清水・石碑にはある「休む」hold が灯篭に無かった(→9.5秒) ③目当ての重み 0.42 が低すぎて**一度も「灯篭へ行こう」と決めていなかった**(清水は2.6。1.8にして→55.5秒)。いまは溜まった熱で切り上げ、3回焼かれると寄らなくなる(1日目41秒→4日目0秒)。★計器の誤り7件目: ハーネスが `lanternWant` を自前で書き直し、新しい学習ゲートを落としていた / **淫魔の絵が死蔵されていた**(`assets/sprites/imp.png` はヒロインと同じ二枚組で git にあるのに、どこからも読まれていなかった) |
| v6.5 | **系統(同系統デッキで特化階層)**。魔物を六系統に括り、同じ系統で固めるほどその系統だけCDが縮む(5枚 −6% 〜 9枚 −30%)。★指揮官の側も直さないと形にならなかった——`autoDirector` は畳みかけ/放出/補充で**魔物の名前を直に並べた列**を見ていて、特化デッキだとその名前が手札に無く場面を素通りしていた。名指しが尽きたら手札から継ぎ足すようにして、場の系統率が 触手36→54% / 胞子35→59%。★CDの縮みは召喚回数では測れない(同条件で ×0.83〜1.08 と踊る)。削れた待ち時間で見ると一夜27秒ぶん |
| v6.5(前段) | **敗北のあとの観測フェーズ**(全員捕獲のあとゲームを進めず、形だけの抵抗を繰り返させる。オーブも経験値も増えない)。押し倒し本文がプレイ中に読めるようになり、**目にした本文は図鑑から読み返せる** |
| v5.4 | **居ない子の名前を呼んでいた**(37箇所のハードコード台詞と掛け合いの片落ち) |
| v5.5 | 合流の朝の経路取り違え(代入順) / **仲間は巻き戻りで増える** |
| v5.6 | 沼よけ / 攻撃で剥がす / 注意力でかわす / 魔核の技を覚える |
| v5.7 | **連続絶頂** / スタミナの引き締め / 催眠Ⅲの到達 / フレイラの火力 |
| v6.3 | **据わった個体が引きずられていた**(番兵が輪から 261px 出て、彼女の 7px 隣に立っていた) / **巣窟の嫌い方が知識で育っていなかった**(innate:1 で最初から 3 に張りつき、決心に 31〜36秒・口の出入り 8.8回) / **巣窟に階ごとの顔つき**(陣/花/光線の数・床の手・住人)と**浅い階の効きを下げる**(1階 0.42 → 15階 1.22) / **ヤミコの武器 3本を作り直し**(形だけ他人の武器だった。闇の輪は一度も描かれていなかった) |
| v6.3b | **媚薬沼の中の宝箱を取りに行けなかった**(避けの免除が押し出しの後ろにあり、浸かった瞬間に押し戻される。加えて深い沼の割り引き ×0.30 で目当てになるのが一瞬だけ)。浅瀬と重なると悪化する——足が鈍るぶん押し戻しが強い |
| v6.4b | **蒸発した媚薬を、噴き出した瞬間に全部吸って即「覚えた」で終わっていた。** うわっ(0.7秒よろける)→吸い込んでむせる(2.8秒)→その間ずっと霧の外へ足が向く→抜けてから「覚えた」の順に。効きは35%だけ即入り、残りはむせている間に。台詞は三段(evap / evapCough / evapLearn)を四人ぶん |
| v6.4 | **「まわりこんできた!」が画面を埋めていた**(一体の大物のための文だが、ボス級の札は6体並ぶ。同時36個→1個) / **魔核の間の認知をループの向きで出し分け**(供が増えていない夜にも心臓が「増えている」と言っていた) / **ヤミコの武器は上限に届くと跳ねる**(Lv8 火力×2.15。届く夜28%) / **媚薬沼は地形ではなく重ね物なので迷いが一度も掛かっていなかった**——縁でためらって言い訳して入る(81/83回) / **伸びしろが尽きたら軽く見て降りる**(満ちた状態で夜153s→69s。★四人では成立しない=残り最少24段) / ヤミコのループ認知 |
| v6.3f | **巣窟から「出る」のに迷っていた。** 迷いの判定が行き先の嫌さしか見ておらず、甘い褥(3.0)から媚薬の澱み(2.0)へ出る時にも立つ。口は澱みで完全に囲まれているので出口が必ず「怖い地形」になる。怯むと澱みを40秒こわがり、唯一の出口を自分で塞ぐ(強制テストで **25/40回 が40秒たっても出られない**)。「いまより嫌な所へ踏み込む時だけ迷う」に直して 0/40回・中央値 40.0s→5.0s |
| v6.3e | **目当てが「なし」の時間を数え直した**。150夜で 3.6%(長い夜 9.2%)、その大半は「拘束・逃走で判断に入っていない」49% と「箱・品を直に追っている」46% ——**数え方の綾だった**。本当に行き先が無い状態(候補ゼロ)だけは残るので最後の受け皿を入れた(強制テストで 109/109 なし → 0/121 なし)。降りると決めた後のジェムの寄り道が**目当てを失った瞬間に最大に戻っていた**のも直した。★器の誤り: `aiDecide` は毎フレームは回らない |
| v6.3d | 降りる気は立ったのに降り口の場所を知らないまま拾い物をして回っていた(探索の価値 0.6 が光茸1.1・清水2.6 に負け続ける)。`EXIT_FIND_WORTH` 4.5 |
| v6.3c | **上位種の解放に深さの錠**(住処の階が開くまで買えない・深いほど値が上がる) / 巣窟のベタベタ(拭うか洗うまで発情が上がり続ける) |
| v6.2 | **拘束の輪・「えいっ」・奥義のスタミナ・壁越しの跳躍**。四点とも実測で通した。3件は原因が思っていた場所と違った(§5) |
| v6.1 | **文章と用語の穴を全数で数えて埋めた**(13〜15階の台詞・押し倒し67件・渦の眠り手・ルミナの手記) / **性癖を9→25件**(閾値は実測から) |
| v6.0g | 待ち手の道が魔核の間へ食い込んで、心臓を壁越しに削れる盤になっていた(同一セッション比較で発覚) |
| v6.0f | **13〜15階の専用ギミック**: 忘れ潟の巻き戻り / 厚みの中の待ち手の道・呼吸・待ちの法 / はじめの夜の見覚え / **新種17体の敗北本文を四人ぶん**(68通り・6.1万字) / ルミナの手記16件(v6.0d の取りこぼし) |
| v6.0e | 骨兵が魔核戦の最中に無料の壁を作っていた(coreWar の番人が抜けていた) / **難度曲線を連続量で測り直し、線0と線6で挟んだ** |
| v6.0d | **手記を全56種 × 三人ぶん**(F20/K50/Y50 を追記) / **種族別の敗北称号を四人ぶん書き下ろし**(224件。三人ぶんは雛形だった) |
| v6.0c | 難度曲線を測り直し(★計測の作法の項を §6 に追記) / 骨兵の上限を人数で / 深階の hp・stam を厚く |
| v6.0b | **クウとヤミコの敗北本文を38種ぶん書き下ろし**(1,334字 / 1,230字 → 40,587字 / 41,021字。ルミナの75%) / 新種14体とボス3体 / 堕ち帯による締めの差し替え |
| v6.0 | **15階層へ**(13 忘れ潟 / 14 厚みの中 / 15 はじめの夜) / **階ごとの地形レシピ**(12階が実は3種類しかなかった) / **新しい床8種**(鏡・紋・糸・霜・胎・澱・忘れ水・苔もどき) / **新しい壁5種**(骨・黒鏡・氷・層・心膜) / **熟れた個体**(深いほど格で押す) / 魔核の難度曲線を直す |
| v5.8 | **媚薬沼をマップチップに**(大小三通り・絵と当たり判定が同じタイル) / フレイラが沼のそばで炎を焚かない・触れる前に落とす / 媚茸・抱き茸・沼の触手の敗北本文(欠落キー3つを含む) / **物語をループの文法で全面書き直し**(30ブロック) / **祭壇に討伐回数の錠**・弱体化をヒロイン別に / **観測記録・図鑑・称号を各人の帳簿に** |

---

## 7-2b. 序盤の難度(v6.3 実測・14日 × 8周)

何も知らない一日目から14日、夜側が素直に組んだデッキ(その日までに開いた札・Lv1〜3)で回した。

| 階 | v6.2 まで | 巣窟を直した後 | 押し倒しの敷居も直した後 |
| --- | --- | --- | --- |
| 1階 | **33%**(17/51) | 13%(5/38) | **11%**(4/37) |
| 2階 | 16%(5/31) | 11%(4/37) | **8%**(3/37) |
| 3階 | 15%(3/20) | 8%(2/24) | **4%**(1/25) |
| 4階 | 20%(2/10) | 17%(2/12) | **0%**(0/12) |

★**捕まった時の体力は、三回とも 96〜100%**。負け方の内訳は
v6.2 まで stamina 12 / charm 1 / **hp 0**。
彼女は殴り倒されているのではなく、力尽きて押し倒されている。
**だから HP や火力を足しても、この負け方はほとんど減らない**——
「初期ステータスが低いのでは」という見立てで攻撃力や体力を触る前に、
必ず `run_early63.js` で負け方の内訳を数えること。

残った1階の敗北4件の相手は 地上ワーム2 / 石の番兵1 / 沼の触手1 と散っている
(v6.2 までは石の番兵が 6/13 で最多だった)。

## 7-2. いまの難度曲線(v6.0g 実測・n=8 × 2帯)

余裕 = 生存秒 ÷ 討伐に要る秒。1.00 を超えれば魔核を討てる。
石段の線は世代と逆を向く(線＝夜側が二連勝した回数 / 世代＝彼女たちが心臓を討った回数)ので、
線0(＝一度も二連敗していない)と線6(＝何度か負けている)の二点で挟んで読む。

| 世代 | 人数 | 最深階 | 余裕(線0) | 討伐(線0) | 余裕(線6) | 討伐(線6) |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 1 | 2 水鏡の洞 | 0.93 | 4/8 | 1.05 | 4/8 |
| 2 | 2 | 4 沈んだ回廊 | 0.83 | 3/8 | 1.03 | 4/8 |
| 4 | 2 | 6 骸の回廊 | **0.60** | 0/8 | 0.75 | 1/8 |
| 6 | 3 | 8 深淵の底 | 0.87 | 1/8 | 0.83 | 1/8 |
| 9 | 4 | 11 渦の縁 | 0.89 | 2/8 | 0.92 | 3/8 |
| 12 | 4 | 14 厚みの中 | 0.90 | 4/8 | 1.04 | 5/8 |

★世代4の線0だけが落ちているのは、**設計どおりの角**と読んでいる。
`PARTY_JOIN` は仲間を `resets:1`(＝二連敗を一度)で寄越し、石段の線も同じ出来事で増える。
つまり「線0 かつ 2人 かつ 世代4」は、**四度勝ったのに一度も二連敗していない**プレイヤーの盤面——
仲間も線も増えないまま、階だけが深くなった状態。
彼女たちが二度負けた瞬間に、仲間と線が同時に入って解消する。勝ち続けることの代償。

★世代12の線6(1.04)には、**直しきれていない残りがある**。v6.0g の遮蔽バグを直して
線0 は 1.02→0.90(baseline 0.91)まで戻ったが、線6 は 1.03→1.04 のまま、
毎秒も 456→373(baseline 301)としか下がっていない。
ただし毎秒の分散は ±94 で、373 と 301 の差はその中に収まる。
**分散より小さい差を根拠に定数を動かすと雑音を追うことになる**(§5)ので、ここは動かさない。
遮蔽そのものは構造で消えていることを確認済み(魔核の間の壁が 47枚→17枚)。
気になるなら n を増やして測り直すこと——定数いじりで詰める場所ではない。

---

## 7-3. 13〜15階の法が、実際に何を消して何を残しているか(v6.0f 実測)

**忘れ潟(13階)の線引き**が、この階のすべて。70秒走らせて、12階(対照)と並べた:

| | 13階 忘れ潟 | 12階(対照) |
| --- | --- | --- |
| 罠の記憶(`trapKnow`) | **24 → 0** | 24 → 24 |
| 種族の知識(`know.met`) | 45 → 59 | 45 → 91 |
| 地形の避け(`zoneKnow`) | 12 → 14.8 | 12 → 12 |
| 性癖(`traits`) | 3 → 4 | 4 → 4 |
| 敏感化の下限 | 30 → 30 | 30 → 30 |

罠の記憶は**まるごと溶ける**ので、同じ凹み・同じ紋・同じ輪に何度でも「初めて」掛かる。
種族の知識は消えるのではなく、覚える速さを削られている(対照の 46 に対し 14 しか伸びない)。
そして**地形の避けも性癖も敏感化の下限も、消えないどころか伸び続ける**——
これが「記憶は消えて、身体だけが残る」の実装形。`run_lethe61.js` が毎回これを並べて出す。

**厚みの中(14階)**は、動く種を出した瞬間に据わったものへ置き換える(`SIT_SUBS`)。
実測で動くものは石の番兵4体だけ(番人は法から除く——据わらせると降り口が永久に開かない)。
呼吸は「切った版との引き算」で +32.4 / 4周期(`run_breath.js`)。
★最深階(＝心臓がそこに居る夜)では法を止める。止めないと魔核戦だけが楽になる(§5)。

**はじめの夜(15階)**は、一晩目に見つけた場所の 8/15 を最初から「知っている」状態で始まる。
`visited` は写さないので、祠は「知っているが開けていない」——開けに行くと抱き茸が居る。
清水の位置は忘れ水(90タイル)、光茸のそばには媚茸。

---

## 7-4. 完成の判定に使う計測(v6.1)

「全部入っている」を主観で言わないための道具。**この4本が全部通ることを一時完成の条件**とする。

| スクリプト | 何を数えるか | 通る条件 |
| --- | --- | --- |
| `run_audit60.js` | 敗北本文・押し倒し・手記・称号・用語・階層・物語・階層の独り言 | 穴 0件 |
| `run_ask60.js` | ゲームが実際に引きに来た文章と、default に落ちた回数 | default 落ち 0 |
| `run_trait61.js` | 性癖が実プレイで刻まれるか | **3回まわした和で 25/25** |
| `run_through61.js` | 一日目から15階まで、通しで遊べるか | 15階到達・ループ・三人合流 |
| `run_grip62.js` | 再拘束の猶予 / 「えいっ」 / 奥義のスタミナ / 壁越しの跳躍 | 猶予破り0・成否とも出る・全部払う・壁越し0 |
| `check_dead.py` | 繋がっていない定義(静的・数秒) | 穴 0件 |
| `run_layout62.js` | 縦持ち三機種で、手札と操作ボタンに指が届くか | はみ出し 0件 |

補助(穴が出た時に、出どころを特定するためのもの):
`run_burst62.js`(「えいっ」の確率を成分ごとに分ける) /
`run_jump62.js`(座標を大きく書き換えた呼び出し元を stack から取る)

★**性癖の判定は「1回の計測で 25/25」ではない。** 四つ(雄臭・映る悦び・絡まる安堵・壺癖)は
150戦で 0〜10回しか刻まれない稀な出来事で、どれが出るかは回ごとに入れ替わる。
実測(15階すべて・150戦を4回):

| 回 | 到達 | 刻まれなかったもの |
| --- | --- | --- |
| 階を10個に間引いた版 | 20/25 | 雄臭・映る悦び・絡まる安堵・擦り合わせ・壺癖 |
| 15階すべて | 24/25 | 絡まる安堵 |
| 糸の閾値を下げた後 | 24/25 | 映る悦び |
| 鏡の閾値も下げた後 | 23/25 | 雄臭・壺癖 |

**三回の和は 25/25** ——つまり全部到達する。1回の数字を根拠に定数を動かすと雑音を追う(§5)。

★分母は「机上の全56種」ではなく **実測**で決める。押し倒し本文は、
実際に拘束を成立させた種(`run_ask60.js` で計測)＋trait に繋留を明記した種の26種が分母で、
全56種ではない——押し倒しに至らない種の本文を数えても意味が無い。

---

## 8. 既知の課題(2026-09 の実プレイ監査より)

エージェント9体＋手作業で約800ゲーム日を走らせた監査の結果。**未着手のものだけ**を挙げます。

### 大きいもの

- ~~手記と種族別称号~~ → v6.0d で三人ぶん、v6.0f で**ルミナ本人ぶん**も埋まった
  (称号は四人ぶん書き下ろし済み)。★v6.0d は「三人をルミナに揃える」作業だったので、
  **揃える先のルミナ自身に新種16件の穴が開いていることに気づけていなかった**。
  「基準に合わせる」形の作業をした時は、基準そのものも数えること
- **帯の滞在時間がプロファイルの逆**。実測で綻び 4.0秒 / 絶頂帯 24.5秒(最長104秒)。
  カタログは「yield を最長に」と定めている
- ~~性癖は25件中9件~~ → v6.1 で25件すべて定義し、刻み口も繋いだ。
  v6.2 で**三回の和で 25/25** を確認(§7-4)。四つは稀な出来事で、
  1回の計測では 23〜24/25 に揺れる
- **降り口の石の番兵は「詰み」ではなく「非常に長い戦い」**(v6.2 で数え直した)。
  番兵が一体でも生きていると `exitGuarded()` が真で、降り口は使えない。
  13/14/15階を各4回、最長400秒:

  | 階 | 番兵 | 結果 |
  | --- | --- | --- |
  | 13階 | 4体 | 4回とも **HP 96〜100% のまま**、42〜72秒で捕まって夜が終わる |
  | 14階 | 4体 | 1回は 148秒で全滅させて降りた。残り3回は 32〜66% まで削って**2回は400秒でも終わらず** |
  | 15階 | 0体 | 最終階なので番兵は居ない |

  ★13階で番兵が無傷なのは強いからではなく、**その前に捕まるから**。
  14階だけ「追ってくるものが居ない」ので長く生き延び、そこで初めてこの戦いに正面から当たる。
  通し260日で「500秒たっても終わらない日」が10日出たのは全部これで、
  **止まっているのではなく、時間制限が無い設計のまま長い夜になっている**。
  番兵のHPか数を深階で緩めるかどうかは、ボス級の設計に関わるので未決

- ~~新種17体の敗北本文~~ → v6.0f で埋まった(17種 × 四人 × 8拍 = 68通り・6.1万字。
  実測で `default` に落ちる組み合わせ 0)
- ~~ヤミコのボス形態(`yamiboss`)だけ捕獲本文が `default`~~ → v6.1 で三人ぶん書き足した
- **場面の題と種族別称号が、種ごとに食い違っている。**
  同じ敗北でも、結果画面の見出しは場面の `title`(フレイラ×ワーム=「濡れた火種」)、
  観測記録の名前は `speciesTitleFor`(同=「掌を巻かれて火を出せない熾」)。
  別々の器なので不具合ではないが、フレイラで**17種が一致・39種が相違**。
  一本化するか、いまのまま二つの名で呼び分けるかは**未決**
- ~~13〜15階の専用ギミック~~ → v6.0f で入って、線引きまで実測できた
  (忘れ潟の巻き戻り / 厚みの中の待ち手の道・呼吸・待ちの法 / はじめの夜の見覚え)。§7-3 参照

### v6.3 で増えた「長い夜」(作者の判断待ち)

通し260日で「500秒たっても終わらない日」が **10日 → 16日** に増えた。
しかも v6.2 では全部が14階の番兵戦だったのに、v6.3 では 2階・4階・5階にも散っている。

- **止まってはいない。** 発情III + 媚薬のベタベタ満 の状態で、開いた降り口から
  5〜7秒で降りられることを作って確かめた(`run_exitlock63.js`)。詰みではない
- **いちばんありそうな筋は、頼まれた変更そのものの裏返し。**
  押し倒しの敷居を浅い階で緩めたので、捕まって早く終わる日が減った
  (260日で捕獲33日=12.7%。v6.2 までは1階だけで33%だった)。
  捕まらない夜は、その分だけ長い
- 長い日の多くは 敵200〜302体 を抱えている。時間制限が無い設計のままの「長い夜」
- ★ただし **4階「沈んだ回廊」が5回**出ている。封印石のパズル階なので、
  石を灯しきれずに長引いている可能性がある。ここだけは別に測る値打ちがある
- どちらも一本ずつの標本(260日×1)なので、10→16 の差そのものは揺れの内かもしれない。
  詰めるなら同じ器で前後を二本ずつ取ること

### 決着: 目当てが「なし」の時間(v6.3e で数え直した)

v6.3d で「30〜60%」と書いた数字は、**数え方の綾がほとんどだった**。
150夜(28858秒)を、判断の拍ごとに訳を残して数え直した結果:

  目当てなし = 全体の 3.6%(400秒を越えた夜だけなら 9.2%)
    49% 拘束・押し倒し・逃走で、判断が目当ての所まで来ていない
    46% 箱・品・ハートを**直に**追っている(`updateGoal` は `if(!target)` でしか
        呼ばれないので、その間 `p.goal` は古いまま=null になる)
     5% 境で怯んで捨てた一拍
     0% 本当に行き先が無い(長い夜では出ない。普通の夜で 3%)

つまり大半は「されている最中」と「箱へ歩いている最中」。作者の判断基準からすれば
直すところは無かった。**それでも「本当に行き先が無い」は、稀だが起きた夜は必ず長い**
(試走で 592秒の夜のうち 198秒)ので、最後の受け皿を入れた。詳細は DESIGN §3-53。

★**器の誤りを二つ踏んだ。記録しておく。**

- **`aiDecide` は毎フレームは回らない**(`THINK_MIN`〜`THINK_MAX` = 0.13〜0.34秒に一度、
  催眠と朦朧でさらに伸びる)。判断の中に印を置いてフレームごとに読むと、
  **85%が「古い印」**になり、意味の無い数字が出る。印は判断の拍ごとに残すこと
- `B.lastCands` も決め直しの瞬間にしか書かれない。フレームごとに読むと
  **別の子の、何秒も前の候補**を読む(実測で同じ文字列が「なし」の 60% に出た)
- いまは `B.dbgCands=true` の時だけ `p.dbgGoal` に訳が残る(`run_goalstat63.js` が読む)

### 次に測ること: 降りる気が立ってからの 467秒

長い夜の本体はこちら。**降りる気は平均98秒で立つのに、そこから夜の終わりまで 467秒ある。**

  63日目 6階 436s  降りる気107s  そのあと降り口を知らないまま **100%**
  56日目 7階 595s  降りる気115s  同 **82%**
  46日目 8階 596s  されている47%(こちらは中身が出ているので直すところではない)

「知らない」以外の内訳(番兵に阻まれている / 拘束されている / 届かない)は、まだ分けていない。
`run_goalstat63.js` に足すのが早い。

### 計測の器そのものの限界(次に測る人へ)

- **`run_wp63.js`(武器の火力×距離)は、彼女を釘付けにして測る。**
  だから「跳び込む武器」は正しく出ない——焔の翼 82点・影渡りの余波 0点は
  **武器が弱いのではなく、測り方がその武器に合っていない**。
  この二つを数字だけ見て「直す」と、直っていないものを直したことになる
- 氷の追い矢は**味方の弾に伴走弾を並べる**札なので、単独で測ると意味が無い(222点)。
  せいすいは投げ先が乱数(本家準拠)なので、n が小さいと 97〜210 と揺れる
- 火の輪のように回る武器は、輪の位相と的の角度で 1485〜2281 と 5割ちかく揺れる。
  同じ数字を二度取ってから差を語ること
- **炎の剣 3541点は「熱が満ちきった理想の値」**。器は的6体を彼女の周りに
  固定して離れないので、フレイラの熱(v6.3)が常に 5/5 になる。
  実際の夜は動くので下がる。強すぎると感じたら `FLAME_DMG`(0.55)を落とす

### 中くらい

- ~~縦持ちで手札の半分と AUTO/速度/撤退が画面外(53要素)~~ →
  v6.2 に測り直したら iPhone SE / iPhone 14 / Pixel 7 の三機種とも
  **はみ出し0件・手札5枚とも画面内・操作ボタン全部届く**(`run_layout62.js`)。
  ★ただし最初の計測では「全機種で下に36〜47px はみ出す」と出た——
  同期の evaluate の中では `ResizeObserver` が走らないため(§5)
- **ヒロイン4人だと、盤の上から2割強が HUD で埋まる。** 実測(縦持ち 390×844):
  体力/スタミナの段が4本で y≈115 まで、状態の札が **8段**で y≈190〜325(盤は613)。
  文字が重なってはいないが、密度は高い。畳むかどうかは作者の判断
- ~~「嗅ぐ(sniff)」は代入が一箇所も無い死にコード~~ → v6.2 で引き金を繋いだ
  (臭いの雲の中で熱があると 1.6 秒足が止まる。`MUSK_SNIFF_CD` ごとに一度)
- ~~えちえちエリアに16日中5日しか入らない~~ → v6.3 で口の前の決心が
  31〜36秒 → 1.3〜4.5秒 になり、知らない巣窟には 30/30 入るようになった。
  一日の中で何度入るかは測り直していない(`wantExit` が中央値50秒で立つ問題は残る)
- オーブは22日で使い道が尽き、エッセンスは106日ぶんある(時間軸が5倍ずれている)

---

## 9. このファイルの育て方

版を出すたびに:

1. **§7 の履歴**に一行足す
2. **§8 の既知の課題**から、直したものを消す。新しく見つけたものを足す
3. 仕様そのものが変わったら(合流の規則・ループの文法・声の設計など)、
   §2〜§4 の**本文を書き換える**。履歴に流さない——ここは常に「いまの姿」であること
4. 落とし穴を新しく踏んだら §5 に足す。**同じ穴を次のAIが踏まないために**
