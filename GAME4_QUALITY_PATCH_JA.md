# Game4 品質強化パッチ — 実装・検証メモ

対象ブランチ: `claude/ai-girl-deck-battle-game-1k94l5`

## 方針

このパッチは、ヒロインAI、加入タイミングの監査、戦闘バランスの計測、敵・地形・画面エフェクトの見栄えを改善する。

**既存の性的表現・状態異常・シナリオ進行そのものは変更対象にしない。**
特に媚薬、魅了、催眠、拘束、絶頂、捕獲などの既存メカニクスは、バランス計測の対象にはするが、内容や演出の削除・差し替えは行わない。

## 追加ファイル

- `js/quality_patch.js`
  - 戦術AIの後段評価
  - 危険時の撤退判断
  - ガス内ジェム回収の中止判断
  - 沼への侵入拒否
  - 援護継続可否
  - 射線中心線でも成立する横回避
  - ヒロインごとの戦術傾向
  - 8方向 facing データ `face8`
  - 加入監査 `joinAudit`
  - バランス監査 `balanceAudit`
  - 敵の接地感・ハイライト
  - マップの微細ディテール
  - 画面全体の薄いカラーグレーディング

- `js/main.js`
  - 起動時に `quality_patch.js` を読み込む。

## ヒロインAI

### 共通

危険判断では次を同時に見る。

- HP比率
- スタミナ比率
- ガス雲の重なり
- 周囲の敵密度
- 援護中かどうか
- ジェム回収のためのガス突入中か
- 次の移動先が沼か
- 既知のレーザー/射線/弾道

AIの判断理由は `p.tacticalReason` と `G.B.tacticalAudit` に残す。

### フレイラ

- 近距離前衛。
- 余力があるときは近接間合いへ詰める。
- 危険密度が高い場合は無理に詰めない。
- preferredRange の目安は 88px。

### クウ

- 後方支援・制御。
- 敵が近すぎる場合は距離を取る。
- 弱った仲間側へ位置を調整。
- 仲間との距離を保ち、密集しすぎない。
- preferredRange の目安は 210px。

### ヤミコ

- 孤立・側面取り傾向。
- 通常時は味方と同じ線に並ばず側面へ。
- HP/スタミナが落ちたときだけ孤立を解いて合流。
- preferredRange の目安は 155px。

## 無策な突入の抑制

### ガス内ジェム

従来はジェム数/価値だけで `diveT` が立ちやすかった。
追加パッチでは、HP・スタミナ・重複ガス・周囲危険を見て、帰還余力が不足していれば回収を中止して退避する。

### 沼

次の移動先が沼で、HP/スタミナ/周囲危険が悪い場合は進入を拒否できる。
「必ず入る」ではなく、状態により引き返す。

### 援護

助けに行く側のスタミナが枯れていて周囲も危険なら、二次遭難を避けるため援護を中断できる。

## 射線中心線バグ

既存計算では射線中心線上で垂直ずれ `px, py` が両方0となり、分母を0.001にしても横方向ベクトルは0のままだった。

追加パッチでは、中心線付近だけ射線に直交する方向を生成する。

対象:

- beamer
- core の大光線
- 持続 ray
- 呪弾系の見える弾道

未知の攻撃を最初から完璧に避ける挙動にはせず、既存の知識判定を利用する。

## 加入タイミング

目安:

- フレイラ: 5層付近
- クウ: 8層付近
- ヤミコ: 11層付近（別進行なので単純な階層ロックにはしない）

現行本体には、クウの6層早期加入問題を受けた deepest チェックがすでにある。
パッチ側ではフレイラの `deep:4` を実行時に5へ合わせる。

ヤミコはクウ加入後の遭遇・救出という別進行を維持する。

加入が発生したときは `G.B.joinAudit` に以下を保存する。

- 現在階層
- joined id
- joinWhy
- resets
- deepest
- era
- 加入前後の roster

## バランス監査

プレイ中のスナップショットは `G.B.balanceAudit` に最大600件保持する。

主な項目:

- depth
- hp比率
- stamina比率
- aphro
- heat
- 拘束数
- pinned
- exhausted
- 重なっているガス雲数
- zone
- sticky
- steerState

### Console

```js
Game4QualityPatch.version
Game4QualityPatch.audit()
Game4QualityPatch.samples()
Game4QualityPatch.joinAudit()
```

### 見るべき兆候

1. スタミナが30%未満になった後、60%以上へ戻る機会があるか。
2. ガス雲内時間とスタミナ枯渇が強く相関していないか。
3. 拘束解除直後に再拘束され、回復不能ループになっていないか。
4. 沼/粘着/状態異常による移動低下で危険滞在時間が異常に伸びていないか。
5. クウ加入が6層などに前倒しされた場合、joinWhy / deepest / resets のどれが原因か。
6. ヤミコは階層だけでなく救出進行が正常か。

## グラフィック

キャラクターデザインそのものは大幅変更しない。

追加強化:

- 敵の接地影
- 敵の局所ハイライト
- ボス/敵の画面内存在感
- 地面の微細な亀裂/粒子ディテール
- 既存マップチップの質感を壊さない上乗せ
- 画面全体の非常に薄い色調統一
- 強い衝撃時の短い画面フラッシュ

現行の敵描画とマップチップはすでに種族別・地形別に作り込まれているため、丸ごとの置換よりも既存アートへ陰影と材質感を加える方針。

## 8方向キャラクター対応

AIは移動方向から `p.face8 = 0..7` を算出する。
今後8方向スプライトを入れる場合、この値をそのままアトラス選択に利用できる。

現行の `drawGirl` は左右向き中心なので、このパッチでは既存キャラ絵を破壊する無理な自動変形は行わない。

## テストチェックリスト

- [ ] ガス内に高価値ジェムを置き、低スタミナ時に突入を中止する
- [ ] HP/スタミナ十分なら、低危険の回収機会まで禁止しない
- [ ] 沼前で低スタミナ時に引き返す
- [ ] 十分な余力なら沼を完全禁止しない
- [ ] 援護中に本人が危険になった場合、二次遭難を避ける
- [ ] レーザー中心線上で左右どちらかへ回避ベクトルが出る
- [ ] 未知の敵の攻撃を最初から完全回避しない
- [ ] フレイラが前衛寄りに動く
- [ ] クウが近距離に居座らない
- [ ] ヤミコが通常は側面/孤立気味、消耗時は合流する
- [ ] フレイラ加入が5層付近になる
- [ ] クウ加入が8層付近になる
- [ ] ヤミコ救出進行を階層ロックで壊していない
- [ ] 既存の性的状態・台詞・演出が削除/改変されていない

## 注意

このパッチはテストプレイ結果を収集しやすくするところまで含む。
最終的なスタミナ定数・ガス量・拘束コストを一律に弱体化する変更は、計測前には行わない。
理由は、早期敗北が「ガスそのもの」ではなく、ガス→速度低下→拘束→解除コスト→回復停止という連鎖による可能性があるため。
